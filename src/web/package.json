{
  "scripts": {
    "start": "vite --port 5174 --host",
    "test": "vitest",
    "test:run": "vitest run",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "report:e2e": "npx playwright show-report playwright-report --host 0.0.0.0 --port 9323"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/jsdom": "^28.0.1",
    "@types/node": "^25.5.0",
    "@vitest/coverage-v8": "^3.2.4",
    "happy-dom": "^20.9.0",
    "jsdom": "^24.1.3",
    "typescript": "^6.0.2",
    "vitest": "^3.0.0"
  }
}
